// Test stub for the `server-only` package. The real module throws when imported
// outside a React Server Component; under Vitest we exercise the route handlers
// directly, so we alias it to this no-op (see vitest.config.ts).
export {};

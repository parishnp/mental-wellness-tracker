// Model IDs isolated here. Gemini IDs evolve — verify against Google AI docs at build time.
export const MODELS = {
  extract: "gemini-2.5-flash",
  insight: "gemini-2.5-pro",
  personalize: "gemini-2.5-flash",
  chat: "gemini-2.5-flash",
} as const;
